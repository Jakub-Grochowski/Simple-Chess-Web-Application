
@{
    ViewBag.Title = "Board";
}
@Styles.Render("~/Content/MyCSS.css")
<h2>Board</h2>
@ViewBag.Board.Size
<table>
    @{ for (int i = 0; i < ViewBag.Board.Size; i++)
        {<tr>
    @{
        for (int j = 0; j < ViewBag.Board.Size; j++)
        {
            <td>+</td>
        }
        }
    </tr>
        }
    }
    </table>
